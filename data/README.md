# Data

This project uses daily GLD OHLCV data.

The raw dataset is downloaded and cached locally during execution and is not committed to the repository. This keeps the repository lightweight while preserving the intended workflow described in the report and notebook.